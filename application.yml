server:
  port: DYNAMICPORT
  address: 0.0.0.0
spring:
  main:
    banner-mode: log
lavalink:
  server:
    password: "helloiamhoang"
    sources:
      youtube: true
      bandcamp: false
      soundcloud: true
      twitch: false
      vimeo: true
      mixer: false
      http: true
      local: true
    bufferDurationMs: 400
    youtubePlaylistLoadLimit: 100
    youtubeSearchEnabled: true
    soundcloudSearchEnabled: true
    gc-warnings: false

metrics:
  prometheus:
    enabled: false
    endpoint: /metrics

sentry:
  dsn: ""

logging:
  file:
    max-history: 2
    max-size: 100MB
  path: /app/logs/

  level:
    root: INFO
    lavalink: INFO
